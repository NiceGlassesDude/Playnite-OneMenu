<Window x:Class="OneMenu.TagSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{DynamicResource LOCOneMenuTagBrowser}" Height="560" Width="760"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{DynamicResource NormalBrush}"
        Foreground="{DynamicResource TextBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="34" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="5" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="#4DFFFFFF" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="34" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{DynamicResource NormalBrushDark}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock Text="&#xE721;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0" />
                <TextBlock Text="{DynamicResource LOCOneMenuTagBrowser}"
                           VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="—" Width="40" Click="Minimize_Click" Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextBrush}" />
                <Button Content="✕" Width="40" Click="Close_Click" Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{DynamicResource LOCOneMenuTagBrowserHint}" TextWrapping="Wrap" />
                <TextBlock x:Name="ResultCountText" Grid.Column="1" Opacity="0.6" Margin="10,0,0,0" />
            </Grid>

            <TextBox x:Name="SearchBox" Grid.Row="1" Margin="0,0,0,6" FontSize="16" Padding="6"
                     TextChanged="SearchBox_TextChanged" />

            <Grid x:Name="BusyPanel" Grid.Row="2" Margin="0,0,0,6" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="BusyText" Grid.Column="0" Opacity="0.7" FontSize="11" Margin="0,0,10,0" VerticalAlignment="Center" />
                <ProgressBar Grid.Column="1" IsIndeterminate="True" Height="4" VerticalAlignment="Center" />
            </Grid>

            <ItemsControl x:Name="ResultsItemsControl" Grid.Row="3"
                          SizeChanged="ResultsItemsControl_SizeChanged"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.ScrollUnit="Pixel"
                          ScrollViewer.CanContentScroll="True">
                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer CanContentScroll="True" Focusable="False"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ItemsControl ItemsSource="{Binding Items}" Focusable="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Click="ResultButton_Click" Tag="{Binding Game}"
                                            ContextMenuOpening="ResultButton_ContextMenuOpening"
                                            ToolTip="{Binding Game.Name}"
                                            Margin="6" Padding="0" Background="Transparent" BorderThickness="0"
                                            Cursor="Hand" Width="120" VerticalAlignment="Top">
                                        <StackPanel>
                                            <Border Width="120" Height="160" Background="#22FFFFFF">
                                                <Image Source="{Binding Cover}" Width="120" Height="160" Stretch="UniformToFill" />
                                            </Border>
                                            <TextBlock Text="{Binding Game.Name}" TextWrapping="Wrap" TextAlignment="Center"
                                                       TextTrimming="CharacterEllipsis" Height="32"
                                                       Margin="0,4,0,0" FontSize="12" />
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
    </Border>
</Window>
