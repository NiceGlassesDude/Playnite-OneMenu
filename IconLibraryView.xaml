<UserControl x:Class="OneMenu.IconLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OneMenu">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HintText" Grid.Row="0" TextWrapping="Wrap" Opacity="0.7" Margin="0,0,0,8" />

        <ListBox x:Name="IconList" Grid.Row="1"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="IconList_SelectionChanged"
                 MouseDoubleClick="IconList_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="104" Margin="4" ToolTip="{Binding FileName}">
                        <Border Width="88" Height="88" Background="#22FFFFFF" CornerRadius="4" HorizontalAlignment="Center">
                            <Image Source="{Binding Image}" Width="64" Height="64" Stretch="Uniform" />
                        </Border>
                        <TextBlock Text="{Binding FileName}" FontSize="11" TextAlignment="Center"
                                   TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
                        <TextBlock Text="{Binding UsageText}" FontSize="10" Opacity="0.6" TextAlignment="Center"
                                   Visibility="{Binding InUse, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock x:Name="EmptyText" Grid.Row="1" Text="{DynamicResource LOCOneMenuIconsEmpty}" TextWrapping="Wrap"
                   Opacity="0.6" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                   Margin="30" IsHitTestVisible="False" Visibility="Collapsed" />

        <TextBlock x:Name="StatusText" Grid.Row="2" TextWrapping="Wrap" Opacity="0.8" Margin="0,8,0,0" Visibility="Collapsed" />

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <WrapPanel Grid.Column="0">
                <Button x:Name="AddButton" Content="{DynamicResource LOCOneMenuIconsAdd}" Padding="12,6" Margin="0,0,6,4"
                        Click="AddButton_Click" />
                <Button x:Name="DeleteButton" Content="{DynamicResource LOCOneMenuIconsDelete}" Padding="12,6" Margin="0,0,6,4"
                        Click="DeleteButton_Click" IsEnabled="False" />
                <Button x:Name="OpenFolderButton" Content="{DynamicResource LOCOneMenuIconsOpenFolder}" Padding="12,6" Margin="0,0,0,4"
                        Click="OpenFolderButton_Click" />
            </WrapPanel>

            <StackPanel x:Name="PickerButtons" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Visibility="Collapsed">
                <Button Content="{DynamicResource LOCOneMenuCancel}" Padding="14,6" Margin="0,0,8,0" Click="CancelButton_Click" />
                <Button x:Name="SelectButton" Content="{DynamicResource LOCOneMenuIconsSelect}" Padding="14,6"
                        Click="SelectButton_Click" IsEnabled="False" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
