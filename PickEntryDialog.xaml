<Window x:Class="OneMenu.PickEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="OneMenu" Height="460" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource NormalBrush}"
        Foreground="{DynamicResource TextBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="34" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="5" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="#4DFFFFFF" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="34" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{DynamicResource NormalBrushDark}">
            <TextBlock x:Name="TitleText" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="✕" Width="40" Click="Cancel_Click" Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptText" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8" />
            <TextBox x:Name="FilterBox" Grid.Row="1" Margin="0,0,0,8" Padding="4"
                     TextChanged="FilterBox_TextChanged" KeyDown="FilterBox_KeyDown" />
            <ListBox x:Name="EntryList" Grid.Row="2" DisplayMemberPath="DisplayText"
                     MouseDoubleClick="EntryList_MouseDoubleClick" />
            <TextBlock x:Name="NewHintText" Grid.Row="3" TextWrapping="Wrap" Opacity="0.6" FontSize="11" Margin="0,8,0,0"
                       Text="{DynamicResource LOCOneMenuPickNewHint}" />

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="{DynamicResource LOCOneMenuCancel}" Padding="14,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button x:Name="ConfirmButton" Padding="14,6" Click="Ok_Click" />
            </StackPanel>
        </Grid>
    </Grid>
    </Border>
</Window>
